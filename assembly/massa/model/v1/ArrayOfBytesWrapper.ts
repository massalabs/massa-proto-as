// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.23.2

import { Writer, Reader, Protobuf } from "as-proto/assembly";

export class ArrayOfBytesWrapper {
  static encode(message: ArrayOfBytesWrapper, writer: Writer): void {
    const items = message.items;
    if (items.length !== 0) {
      for (let i: i32 = 0; i < items.length; ++i) {
        writer.uint32(10);
        writer.bytes(items[i]);
      }
    }
  }

  static decode(reader: Reader, length: i32): ArrayOfBytesWrapper {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new ArrayOfBytesWrapper();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.items.push(reader.bytes());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  items: Array<Uint8Array>;

  constructor(items: Array<Uint8Array> = []) {
    this.items = items;
  }
}

export function encodeArrayOfBytesWrapper(
  message: ArrayOfBytesWrapper
): Uint8Array {
  return Protobuf.encode(message, ArrayOfBytesWrapper.encode);
}

export function decodeArrayOfBytesWrapper(
  buffer: Uint8Array
): ArrayOfBytesWrapper {
  return Protobuf.decode<ArrayOfBytesWrapper>(
    buffer,
    ArrayOfBytesWrapper.decode
  );
}
