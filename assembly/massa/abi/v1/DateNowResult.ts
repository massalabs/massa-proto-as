// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.21.12

import { Writer, Reader, Protobuf } from "as-proto/assembly";

export class DateNowResult {
  static encode(message: DateNowResult, writer: Writer): void {
    writer.uint32(9);
    writer.double(message.dateNow);
  }

  static decode(reader: Reader, length: i32): DateNowResult {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new DateNowResult();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.dateNow = reader.double();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  dateNow: f64;

  constructor(dateNow: f64 = 0.0) {
    this.dateNow = dateNow;
  }
}

export function encodeDateNowResult(message: DateNowResult): Uint8Array {
  return Protobuf.encode(message, DateNowResult.encode);
}

export function decodeDateNowResult(buffer: Uint8Array): DateNowResult {
  return Protobuf.decode<DateNowResult>(buffer, DateNowResult.decode);
}
