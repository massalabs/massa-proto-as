// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.23.3

import { Writer, Reader, Protobuf } from "as-proto/assembly";

export class CheckBlake3HashRequest {
  static encode(message: CheckBlake3HashRequest, writer: Writer): void {
    writer.uint32(10);
    writer.bytes(message.toCheck);
  }

  static decode(reader: Reader, length: i32): CheckBlake3HashRequest {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new CheckBlake3HashRequest();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.toCheck = reader.bytes();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  toCheck: Uint8Array;

  constructor(toCheck: Uint8Array = new Uint8Array(0)) {
    this.toCheck = toCheck;
  }
}

export function encodeCheckBlake3HashRequest(
  message: CheckBlake3HashRequest
): Uint8Array {
  return Protobuf.encode(message, CheckBlake3HashRequest.encode);
}

export function decodeCheckBlake3HashRequest(
  buffer: Uint8Array
): CheckBlake3HashRequest {
  return Protobuf.decode<CheckBlake3HashRequest>(
    buffer,
    CheckBlake3HashRequest.decode
  );
}
