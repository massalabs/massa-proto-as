// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.22.2

import { Writer, Reader, Protobuf } from "as-proto/assembly";

export class TransferCoinsForResult {
  static encode(message: TransferCoinsForResult, writer: Writer): void {}

  static decode(reader: Reader, length: i32): TransferCoinsForResult {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new TransferCoinsForResult();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  constructor() {}
}

export function encodeTransferCoinsForResult(
  message: TransferCoinsForResult
): Uint8Array {
  return Protobuf.encode(message, TransferCoinsForResult.encode);
}

export function decodeTransferCoinsForResult(
  buffer: Uint8Array
): TransferCoinsForResult {
  return Protobuf.decode<TransferCoinsForResult>(
    buffer,
    TransferCoinsForResult.decode
  );
}
