// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.23.2

import { Writer, Reader, Protobuf } from "as-proto/assembly";
import { NativeAddress } from "../../model/v1/NativeAddress";

export class NativeAddressFromStringResult {
  static encode(message: NativeAddressFromStringResult, writer: Writer): void {
    const convertedAddress = message.convertedAddress;
    if (convertedAddress !== null) {
      writer.uint32(10);
      writer.fork();
      NativeAddress.encode(convertedAddress, writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): NativeAddressFromStringResult {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new NativeAddressFromStringResult();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.convertedAddress = NativeAddress.decode(
            reader,
            reader.uint32()
          );
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  convertedAddress: NativeAddress | null;

  constructor(convertedAddress: NativeAddress | null = null) {
    this.convertedAddress = convertedAddress;
  }
}

export function encodeNativeAddressFromStringResult(
  message: NativeAddressFromStringResult
): Uint8Array {
  return Protobuf.encode(message, NativeAddressFromStringResult.encode);
}

export function decodeNativeAddressFromStringResult(
  buffer: Uint8Array
): NativeAddressFromStringResult {
  return Protobuf.decode<NativeAddressFromStringResult>(
    buffer,
    NativeAddressFromStringResult.decode
  );
}
